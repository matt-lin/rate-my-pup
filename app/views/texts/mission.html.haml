%div{:class => "container text-container"}
  %div{:class => "header"}
    %h1 Our Mission 
    %h3 
      - @mission_statement_sections.each do |section|
        = section.where(:title => 'Our Mission').text
    %center
    %a{:class => "btn btn-primary", :href => root_path}
      Go Back to Home Page
